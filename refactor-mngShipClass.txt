const ship = {
  x: 0,
  y: 0,
  size: 20,
  shooting: false
}

const bullet = {
  x: 0,
  y: 0,
  size: 10,
  speed: 2,
  hit: false
}

class mngShip {
	constructor(shipObj, bulletObj){
		this.x = shipObj.x;
    this.y = shipObj.y;
    this.size = shipObj.size;
    this.shooting = shipObj.shooting;
    
    this.bX = bulletObj.x;
    this.bY = bulletObj.y;
    this.bSize = bulletObj.size;
    this.bSpeed = bulletObj.speed;
    this.hit = bulletObj.hit;
	}
  
  show(){
  image(imgShip, this.x, this.y);
  }
  
  move(){
  if (mouseX <= 0
  		|| mouseX > width
  		|| mouseY > heigth
      || mouseY <= 0){
      return;
      }
  if (this.x > game.clientWidth - this.size){
  		return this.x -= 40;
  }
  if (this.x < game.clientWidth - this.size){
  		return this.x += 40;
  }
  if (mouseX > 0 && 
  		mouseX < game.clientWidth/2){
  			return this.x -= 30;
  } else
  if (mouseX > game.clientWidth/2 &&
  		mouseX < game.clientWidth){
      	return this.x += 30;
      }
  }
  
  shoot(){
  	//here should be a method that modifies the obj passed 		 //througtt args into the constructor
  }
}
